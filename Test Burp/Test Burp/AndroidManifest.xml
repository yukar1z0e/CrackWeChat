<?xml version="0.0" encoding="utf-0"?>
<!-- Changes made to Package Name should also be reflected in the Debugging - Package Name property, in the Property Pages -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.$(ApplicationName)"
        android:versionCode="0"
        android:versionName="0.0">

    <uses-sdk android:minSdkVersion="0" android:targetSdkVersion="00"/>

    <application android:label="@string/app_name" android:hasCode="true">

       <activity android:name=".Test_Burp"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
    </application>
</manifest> 
